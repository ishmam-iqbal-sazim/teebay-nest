import { Title } from "@mantine/core";

const NoteToSelf = () => {
  return (
    <div>
      <Title color="skyblue" order={1}>
        CONVERT DUPLICATE CODE TO REUSABLE COMPONENTS
      </Title>
    </div>
  );
};

export default NoteToSelf;
